<!-- 此文件由机器翻译自 installation.md -->

# FluidSim 安装

## 要求

-Python >= 3.9
- 推荐虚拟环境

## 安装方法

### 基本安装

使用 uv 安装 Fluidsim：

```bash
uv pip install fluidsim
```

### 具有 FFT 支持（伪谱求解器需要）

大多数 Fluidsim 求解器使用基于傅里叶的方法并需要 FFT 库：

<<<代码块_1>>>

这将安装 Fluidfft 和 pyfftw 依赖项。

### 使用 MPI 和 FFT（用于并行模拟）

对于高性能并行计算：

<<<代码块_2>>>

注意：这会触发 mpi4py 的本地编译。

## 环境配置

### 输出目录

设置环境变量来控制模拟数据的存储位置：

<<<代码块_3>>>

### FFT 方法选择

指定 FFT 实现（可选）：

<<<代码块_4>>>

## 验证

测试安装：

<<<代码块_5>>>

## 无需身份验证

FluidSim 不需要 API 密钥或身份验证令牌。